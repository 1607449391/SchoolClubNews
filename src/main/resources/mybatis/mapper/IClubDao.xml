<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssm.schoolclubnews.dao.IClubDao">

    <insert id="insertClub" parameterType="Club">
        INSERT INTO tpl_club VALUES ()
    </insert>

    <update id="updateClub" parameterType="Club">
        UPDATE tpl_club SET
        WHERE
    </update>

    <select id="getClubById" parameterType="java.lang.String">
        SELECT * FROM tpl_club where id = #{id}
    </select>

    <select id="getClubs" resultType="Club">
        SELECT * FROM tpl_club
    </select>

</mapper>