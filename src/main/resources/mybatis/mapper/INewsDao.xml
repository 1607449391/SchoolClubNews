<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssm.schoolclubnews.dao.INewsDao">

    <insert id="insertNews" parameterType="News">
        INSERT INTO tpl_news VALUES (
        )
    </insert>

    <update id="updateNews" parameterType="News">
        UPDATE tpl_news SET

        WHERE id = #{news.id}
    </update>

    <select id="getNewsById" parameterType="java.lang.String">
        SELECT * FROM tpl_news WHERE id = #{id}
    </select>

    <select id="getNewsByCondiations">
        SELECT * FROM tpl_news
    </select>

</mapper>